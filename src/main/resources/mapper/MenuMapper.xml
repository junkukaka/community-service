<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.community.aspn.menu.mapper.CommunityMenuMapper">
    <select id="selectMenuById2" resultType="com.community.aspn.pojo.sys.CommunityMenu">
        select * from community_menu where id = #{id} order by sort
    </select>

    <select id="selectMenuByTier" resultType="com.community.aspn.pojo.sys.CommunityMenu">
        select * from community_menu where tier = #{tier} and use_yn = 'Y' order by sort
    </select>

    <select id="selectByFather" resultType="com.community.aspn.pojo.sys.CommunityMenu">
        select * from community_menu where father = #{father} and use_yn = 'Y' order by sort
    </select>
</mapper>